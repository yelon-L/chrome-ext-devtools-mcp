# Chrome Extension Debug MCP - Environment Variables

# ==========================================
# Node环境配置
# ==========================================
# 设置为 'production' 启用生产模式
# NODE_ENV=development

# ==========================================
# 错误详细程度配置
# ==========================================
# 控制错误消息的详细程度
# 
# 可选值:
#   - minimal: 仅显示用户友好消息（生产环境推荐）
#   - standard: 显示错误类型和基本信息
#   - verbose: 显示完整技术细节，包括stack trace（开发环境推荐）
#
# 默认值:
#   - 开发环境 (NODE_ENV != production): verbose
#   - 生产环境 (NODE_ENV == production): minimal
#
# ERROR_VERBOSITY=verbose

# ==========================================
# 日志级别配置
# ==========================================
# 控制日志输出级别
# 可选值: DEBUG, INFO, WARN, ERROR
# LOG_LEVEL=INFO

# ==========================================
# Stdio 模式配置
# ==========================================
# Stdio 模式空闲超时时间（毫秒）
# 0 = 永不超时（适合开发环境）
# 默认 1800000（30 分钟）
# 注意：此配置仅对 stdio 模式生效，不影响 streamable 和 multi-tenant 模式
# STDIO_IDLE_TIMEOUT=1800000

# ==========================================
# Multi-Tenant 模式配置
# ==========================================

# ------------------------------------------
# 服务器配置
# ------------------------------------------
# HTTP 服务器监听端口
# PORT=32122

# ------------------------------------------
# 存储配置
# ------------------------------------------
# 存储类型：jsonl（文件） 或 postgresql（数据库）
# STORAGE_TYPE=jsonl

# JSONL 文件存储配置（当 STORAGE_TYPE=jsonl 时使用）
# 数据存储目录
# DATA_DIR=./.mcp-data
# 日志文件名
# LOG_FILE_NAME=store-v2.jsonl
# 快照阈值（记录数量达到此值时触发快照）
# SNAPSHOT_THRESHOLD=10000
# 是否启用自动压缩（true/false）
# AUTO_COMPACTION=true

# PostgreSQL 数据库配置（当 STORAGE_TYPE=postgresql 时使用）
# 数据库主机地址
# DB_HOST=192.168.0.205
# 数据库端口
# DB_PORT=5432
# 数据库名称
# DB_NAME=mcp_extdebug
# 数据库用户名
# DB_USER=admin
# 数据库密码
# DB_PASSWORD=admin
# 最大连接数
# DB_MAX_CONNECTIONS=20
# 空闲超时时间（毫秒）
# DB_IDLE_TIMEOUT=30000

# ------------------------------------------
# 会话配置
# ------------------------------------------
# 会话超时时间（毫秒），默认 1 小时
# SESSION_TIMEOUT=3600000
# 会话清理间隔（毫秒），默认 1 分钟
# SESSION_CLEANUP_INTERVAL=60000
# 最大会话数量（可选，不设置则无限制）
# MAX_SESSIONS=

# ------------------------------------------
# 浏览器连接池配置
# ------------------------------------------
# 浏览器健康检查间隔（毫秒）
# BROWSER_HEALTH_CHECK_INTERVAL=30000
# 最大重连尝试次数
# MAX_RECONNECT_ATTEMPTS=3
# 重连延迟时间（毫秒）
# RECONNECT_DELAY=5000
# 连接超时时间（毫秒）
# CONNECTION_TIMEOUT=30000
# 浏览器检测超时时间（毫秒）
# BROWSER_DETECTION_TIMEOUT=3000

# ------------------------------------------
# 性能配置
# ------------------------------------------
# API 缓存生存时间（毫秒）
# API_CACHE_TTL=30000
# API 缓存最大条目数
# API_CACHE_MAX_SIZE=500
# 性能监控缓冲区大小
# MONITOR_BUFFER_SIZE=1000
# 连接时间缓冲区大小
# CONNECTION_TIMES_BUFFER_SIZE=100

# ------------------------------------------
# 安全配置
# ------------------------------------------
# IP 白名单（逗号分隔，支持 CIDR 和通配符）
# 示例：192.168.1.0/24,10.0.0.*,127.0.0.1
# 不设置则允许所有 IP 访问
# ALLOWED_IPS=

# CORS 允许的来源（逗号分隔）
# 设置为 * 允许所有来源
# ALLOWED_ORIGINS=*

# ------------------------------------------
# 实验性功能（不建议在生产环境启用）
# ------------------------------------------
# 启用 CDP 混合架构（实验性）
# USE_CDP_HYBRID=false
# 启用 CDP 操作（实验性）
# USE_CDP_OPERATIONS=false
